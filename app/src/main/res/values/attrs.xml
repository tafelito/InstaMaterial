<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>

    <!-- Day/Night Mode Colors -->
    <attr name="activeColor" format="reference|color" />
    <attr name="primaryColor" format="reference|color" />
    <attr name="ruleColor" format="reference|color" />
    <attr name="secondaryColor" format="reference|color" />
    <attr name="thirdColor" format="reference|color" />
    <attr name="highlightColor" format="reference|color" />
    <attr name="stickyColor" format="reference|color" />

</resources>
